general {
    hide_cursor = true
}

background {
    monitor =
    path = $WALLPAPER
    blur_size = 7
    blur_passes = 3
    brightness = 0.5
}

label {
    monitor =
    text = Locked
    text_align = center
    color = rgba(200, 200, 200, 1.0)
    font_size = 64
    font_family = JetBrains Mono Bold
    rotate = 0

    position = 0, 80
    halign = center
    valign = center
}
